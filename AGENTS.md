For all python commands use `uv` tool

Each modification in the code must have a "proposal" file in the `proposals` folder, with at least the following information (could have more):

- Goal
- Proposed Changes
- Implementation Steps
- Dependencies

Under "proposed changes" section, if there are new functions, must be their expected input and output